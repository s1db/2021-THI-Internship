array[AGENTS] of par int: old_score;constraint sum(a in AGENTS) ( bool2int(agent_prefers(a, score[a], old_score[a]) ) ) >= max(AGENTS) div 2;var 0..max(AGENTS):  preferred_by; var int: num_voters; constraint num_voters= max(AGENTS);constraint preferred_by = sum(a in AGENTS) ( bool2int(agent_prefers(a, score[a], old_score[a]) ) );constraint (x != 1 \/ y != 3 \/ control != A);constraint (x != 2 \/ y != 2 \/ control != B);